<h1>Connexion</h1>

<%= form_with(model: @user, url: login_path) do |form| %>
  <div>
    <%= form.label(:email) %>
    <%= form.text_field(:email) %>
  </div>
  <div>
    <%= form.label(:password) %>
    <%= form.password_field(:password) %>
  </div>
  <div>
    <%= form.submit("Se connecter") %>
  </div>
<% end %>

<div>Si vous n'avez pas de compte, vous pouvez <%= link_to "en créer un", register_path %>.</div>
